
import React from 'react'
import PropTypes from 'prop-types'

const droppable =(component) => (provided) props => {
    return (
        <div>
            
        </div>
    )
}

droppable.propTypes(component) => (provided) = {

}

export default droppable
(component) => (provided)